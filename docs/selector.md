之前的插件目前实现了录制功能，但是我感觉不是我想要的效果，因为这个插件的底层是提取交互元素同时注册标签，这样每个可交互元素就有了唯一标签，但是问题在于如果页面结构变化或者页面刷新，录制功能就不可复用了，所以如果要实现网页自动化操作，重点就在于选择器的预定义和数据流，也就是告诉ai它可以操作的元素以及如何操作，darado定位元素的方法我没细看，但是我个人感觉想要实现用户友好的脚本制作过程，那就不能让用户来制作选择器，用户应该只负责告诉ai如何操作以及元素之间的逻辑，所以我现在正在尝试实现可复用的、用户可用的选择器制作以及元素逻辑的流通编辑和数据定义的插件。
之前的插件已经在github上开源了https://github.com/CumuloCumulo/Calcifer，下面是新插件的选择器生成功能的演示，目前大体原理是先提取所有（这里先实现的是input)可交互元素，接着用户选择需要使用的元素，接着插件根据用户选择的元素，找到该元素的容器，接着获取容器类别和该容器在其父容器中的序列数，（因为其实页面的大体结构都是固定的，但是具体的元素无法用选择器准确选中）用户下次获取交互元素时首先通过选择器获取记录的容器，然后在容器内通过input选择器寻找input