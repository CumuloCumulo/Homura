关于用户操作的记录，可不可以使用的时候自动开启automa中构造选择器的功能，通过选择器触发，可以直接验证该元素可否由选择器触发
关于选择器的相关内容，主要分为两点：选择器的生成、选择器的使用
选择器是否需要多个，如何生成时判断需要哪种类型，可不可以让ai来判断
何时需要根据文本生成临时选择器：流程中根据用户点击生成选择器框架，ai具体执行时先读取再添参数
延时如何加入，如何感知页面元素已加载完成
最重要的几类操作：
1.点击
2.读取
何时需要全部页面读取
3.填写
4.页面跳转

构建选择器时需要注意该元素是单一元素还是并列元素中的一个，来判断选择器具体的使用方法
automa中这个ui可以参考


录制时动作集如何让ai处理，
人如何修改

必需支持单步执行，执行后需要获取成功与否信息

页面操作需要封装成函数，并留出ai填写的参数


原子化工具集应该是缺参的，当agent开始一个原子化工具时，首先判断是否需要填参，需要的话首先获取当前页面文本，填参后开始执行，验证反馈后选择下一个使用的原子化工具