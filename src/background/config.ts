/**
 * =============================================================================
 * Background Configuration
 * =============================================================================
 *
 * 集中管理所有配置常量，避免硬编码
 */

export const CONFIG = {
  // ==========================================================================
  // 超时配置
  // ==========================================================================
  TIMEOUT: {
    /** 元素查找超时（毫秒） */
    ELEMENT_FIND: 15000,
    /** DOM 稳定性检测超时（毫秒） */
    DOM_STABILITY: 10000,
    /** 页面就绪等待超时（毫秒） */
    PAGE_READY: 10000,
    /** Content Script 就绪超时（毫秒） */
    CONTENT_SCRIPT_READY: 5000,
    /** 轮询间隔（毫秒） */
    POLL_INTERVAL: 300,
  },

  // ==========================================================================
  // 重试配置
  // ==========================================================================
  RETRY: {
    /** 最大重试次数 */
    MAX_ATTEMPTS: 5,
    /** 重试延迟（毫秒）- 指数退避 */
    DELAYS: [200, 400, 800, 1500, 3000],
  },

  // ==========================================================================
  // 延迟配置
  // ==========================================================================
  DELAY: {
    /** 同一页面工具间延迟（毫秒） */
    SAME_TAB_TOOL: 1000,
    /** 新标签页切换后延迟（毫秒） */
    NEW_TAB_CONTEXT: 1500,
    /** Content Script 注入后等待（毫秒） */
    POST_INJECTION: 500,
    /** UI 状态重置延迟（毫秒） */
    UI_RESET: 2000,
  },

  // ==========================================================================
  // 轮询配置
  // ==========================================================================
  POLLING: {
    /** 执行状态检查间隔（毫秒） */
    STATE_CHECK: 500,
    /** Content Script 就绪检查间隔（毫秒） */
    CONTENT_SCRIPT: 200,
  },

  // ==========================================================================
  // 存储键
  // ==========================================================================
  STORAGE: {
    /** 执行状态存储键 */
    EXECUTION_STATE: 'homura_execution_state',
    /** 当前工具集存储键 */
    CURRENT_TOOLKIT: 'homura_current_toolkit',
    /** 工具集列表存储键 */
    TOOLKITS: 'homura_toolkits',
    /** 工具更新存储键前缀 */
    TOOL_UPDATE_PREFIX: 'homura_tool_update_',
  },
} as const;
